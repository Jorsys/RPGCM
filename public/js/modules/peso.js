// Funciones para gestionar el peso del personaje (simplificado)

// Función para actualizar el peso total (ahora simplificada)
function actualizarPesoTotal() {
  // Esta función ahora no hace nada, pero la mantenemos para evitar errores
  // en las referencias existentes
  return
}

// Exportar funciones
export { actualizarPesoTotal }
